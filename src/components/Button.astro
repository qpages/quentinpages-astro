---
import { cn } from "../lib/cn";

type Variant = "primary" | "secondary";
type Size = "default" | "compact";

interface Props {
  href: string;
  variant?: Variant;
  size?: Size;
  pulse?: boolean;
  class?: string;
}

const {
  href,
  variant = "primary",
  size = "default",
  pulse = false,
  class: className,
} = Astro.props;

const variants: Record<Variant, string> = {
  primary:
    "bg-text-primary text-background hover:opacity-90 border-transparent",
  secondary:
    "bg-transparent text-text-primary border-border hover:bg-surface-hover",
};

const sizes: Record<Size, string> = {
  default: "px-5 py-2.5 text-sm font-medium",
  compact: "px-3.5 py-1.5 text-[0.8125rem] font-medium tracking-[-0.01em]",
};
---

<a
  href={href}
  class={cn(
    "inline-flex w-fit items-center justify-center rounded-[var(--radius-pill)] border",
    sizes[size],
    pulse ? "cta-pulse" : "transition-opacity",
    variants[variant],
    className,
  )}
>
  <slot />
</a>
